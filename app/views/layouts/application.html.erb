<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <title>Yield Blue Recommendation Portal</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>
<div class="navbar navbar-inverse">
  <div class="container">
    <div class="navbar-header">
      <%= link_to 'Yield Blue', root_path, class: 'navbar-brand' %>
    </div>
    <ul class="nav navbar-nav">
      <li class="hide"><%= link_to 'Videos', root_path %></li>
      <li class="hide"><%= link_to 'Add Video', new_video_path %></li>
      <% if current_user %>
        <li class="hide"><%= link_to 'Upload Video', new_video_upload_path %></li>
      <% end %>
      <li><%= link_to 'Get Recommendations', recommendation_index_path %></li>
    </ul>
    <ul class="nav navbar-nav pull-right hide">
      <% if current_user %>
        <li><span><%= current_user.name %></span></li>
        <li><%= link_to 'Log Out', logout_path, method: :delete %></li>
      <% else %>
        <li><%= link_to 'Log In', '/auth/google_oauth2' %></li>
      <% end %>
    </ul>
  </div>
</div>

<div class="container">
  <% flash.each do |key, value| %>
    <div class="alert alert-<%= key %>">
      <%= value %>
    </div>
  <% end %>
</div>

<%= yield %>

</body>
</html>
